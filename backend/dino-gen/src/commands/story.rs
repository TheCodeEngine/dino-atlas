pub async fn run(
    _pb_url: &str,
    _gemini_key: &Option<String>,
    _piper_bin: &str,
    _piper_model: &str,
    _family_id: &str,
) {
    tracing::info!("Story generation not yet implemented");
    tracing::info!("Will: fetch today's dinos → Gemini story → Piper TTS → PocketBase");
}
