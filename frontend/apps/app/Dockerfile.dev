FROM node:22-alpine
RUN corepack enable && corepack prepare pnpm@9 --activate
WORKDIR /app
COPY pnpm-workspace.yaml package.json pnpm-lock.yaml* ./
COPY packages/ packages/
COPY apps/ apps/
RUN pnpm install
EXPOSE 5173
CMD ["pnpm", "--filter", "@dino-atlas/app", "dev"]
