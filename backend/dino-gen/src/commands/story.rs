pub async fn run(
    _pb_url: &str,
    _gemini_key: &Option<String>,
    _api_url: &str,
    _family_id: &str,
) {
    tracing::info!("Story generation not yet implemented");
    tracing::info!("Will: fetch today's dinos → Gemini story → TTS API → PocketBase");
}
